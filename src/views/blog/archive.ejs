<section class="space-y-6">
  <div class="wp-panel-strong overflow-hidden">
    <div class="p-6 sm:p-8">
      <p class="text-xs font-semibold uppercase tracking-[0.4em] text-brand-600">Archive</p>
      <h1 class="mt-3 text-2xl font-semibold text-slate-950 sm:text-3xl"><%= archiveTitle %></h1>
      <p class="mt-2 max-w-2xl text-sm leading-6 text-slate-500 dark:text-slate-400">
        Kumpulan artikel yang tersusun rapi dan tetap enak dibaca di layar kecil maupun besar.
      </p>
    </div>
  </div>

  <div class="grid gap-5 sm:grid-cols-2 xl:grid-cols-3">
    <% if (posts.length) { %>
      <% posts.forEach((post) => { %>
        <article class="group wp-panel overflow-hidden h-full transition-all duration-200 hover:-translate-y-0.5 hover:shadow-md">
          <a href="/posts/<%= post.slug %>" class="block">
            <div class="aspect-[16/10] overflow-hidden bg-[color:var(--wp-surface-2)]">
              <% if (post.featuredImage) { %>
                <img src="<%= post.featuredImage %>" alt="<%= post.title %>" class="h-full w-full object-cover transition-transform duration-300 group-hover:scale-105" loading="lazy" />
              <% } else { %>
                <div class="flex h-full w-full items-center justify-center text-slate-400">
                  <i class="fas fa-image text-3xl"></i>
                </div>
              <% } %>
            </div>
          </a>
          <div class="p-5">
            <h2 class="text-lg font-semibold leading-snug text-slate-950 transition-colors group-hover:text-brand-600 dark:text-white">
              <a href="/posts/<%= post.slug %>"><%= post.title %></a>
            </h2>
            <p class="mt-3 line-clamp-3 text-sm leading-6 text-slate-600 dark:text-slate-300"><%= post.subheadline || post.excerpt || '' %></p>
            <div class="mt-4 flex flex-wrap items-center gap-3 text-xs text-slate-500 dark:text-slate-400">
              <span><i class="far fa-clock mr-1"></i><%= post.readingTime || "2" %> min read</span>
              <span><i class="fas fa-eye mr-1"></i><%= post.viewCount || 0 %> views</span>
            </div>
          </div>
        </article>
      <% }) %>
    <% } else { %>
      <div class="sm:col-span-2 xl:col-span-3 rounded-3xl border border-dashed border-[color:var(--wp-border)] bg-[color:var(--wp-surface-2)] p-8 text-sm text-slate-500 dark:text-slate-400">
        Belum ada artikel pada kategori ini.
      </div>
    <% } %>
  </div>

  <%- include('../partials/pagination', { pagination }) %>
</section>
